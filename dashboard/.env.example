# Copy to .env.local. Next.js does not read the repo-root .env; the dashboard
# process needs its own copy of these two values.
BACKEND_URL=http://localhost:8000
DASHBOARD_API_KEY=changeme-dashboard-key
