"""Pure calculation and rule-coverage parsing for the metrics page.

Distinct from backend/app/metrics.py (NFR-4's DashboardVisibilityTracker,
unrelated). Nothing in this package touches ES, HTTP, or the Store
protocol -- see calc.py and coverage.py docstrings.
"""
